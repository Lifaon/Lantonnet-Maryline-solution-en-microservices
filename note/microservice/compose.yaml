services:
  mongodb:
    image: mongo:latest
    environment:
      - MONGO_INITDB_ROOT_USERNAME=user
      - MONGO_INITDB_ROOT_PASSWORD=pass
      - MONGO_INITDB_DATABASE=medilabo
    ports:
      - 27017:27017
    volumes:
      - db:/data/db
volumes:
  db: